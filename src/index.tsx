import * as React from 'react';
import { render } from 'react-dom';
import './index.css';
const App = () => (
  <div>
    <h2>Welcome to React typescript starter with babel7</h2>
  </div>
);

render(<App />, document.getElementById('app'));
